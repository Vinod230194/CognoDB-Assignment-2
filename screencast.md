Screencast script for submission

1. Introduction (10s)
   - Say your name and the demo objective: a small graph app using CognoDB.

2. Show README (10s)
   - Open README and point to setup and run instructions.

3. Run the app (30s)
   - In terminal, set env vars (or show launchSettings), run `dotnet run` or `.\demo.ps1`.
   - Show console output: connectivity, seeding, sample queries.

4. Start web UI (15s)
   - In the console type `web` (if interactive) or open http://localhost:5000.
   - Show people list, query friends by entering 'Alice'.

5. Show queries (15s)
   - Open `queries.cypher` and highlight multi-hop and parameterized queries.

6. Wrap up (10s)
   - Mention where code lives and next steps.

Notes
- Keep recording under 2 minutes. Focus on clarity and the core deliverables.
